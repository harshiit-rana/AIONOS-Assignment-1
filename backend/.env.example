# Provide ONE of these. Groq is free at console.groq.com/keys and is the default.
GROQ_API_KEY=
# GROQ_MODEL=openai/gpt-oss-120b     # default; `python check_llm.py` lists alternatives

# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# With no key set the agent runs its deterministic pipeline and still works.
